# Ajax Interceptor Tools 使用说明（最简版）

本工具是一个 Chrome 浏览器插件，用于拦截和修改网页请求。下面教你从零开始，三步搞定安装。

> 适用人群：非技术人员
> 准备工作：一台装了 Chrome 浏览器的电脑

---

## 第一步：下载压缩包

1. 打开下载页面：[https://github.com/robbiemie/smart-chrome-tool/releases](https://github.com/robbiemie/smart-chrome-tool/releases)
2. 在页面下方找到最新版本的 `Assets` 区域，点击其中的 `smart-chrome-tool-vX.X.X.zip` 文件开始下载（`vX.X.X` 为版本号，下载最新版即可）。
3. 保存到电脑上方便找的位置，例如「下载」文件夹或桌面。

---

## 第二步：解压压缩包

Chrome 不能直接使用 zip 压缩包，必须先解压成一个文件夹。

### Windows 系统
1. 右键点击下载好的 zip 文件（例如 `smart-chrome-tool-v0.0.4.zip`）。
2. 选择「解压到当前文件夹」或「全部解压」。
3. 解压后会出现一个同名文件夹，记住这个文件夹的位置。

### Mac 系统
1. 双击下载好的 zip 文件。
2. 系统会自动解压出一个同名文件夹。
3. 记住这个文件夹的位置（建议拖到「应用程序」或「文档」文件夹中，方便以后查找）。

> ⚠️ 注意：解压后的文件夹不要删除，插件运行时需要一直使用它。建议把它放到一个不会误删的固定位置。

---

## 第三步：在 Chrome 中加载插件

1. 打开 Chrome 浏览器。
2. 在地址栏输入下面的内容，然后回车：
   ```
   chrome://extensions
   ```
3. 在页面右上角找到「**开发者模式**」开关，把它打开。
4. 开关打开后，页面左上角会出现几个按钮，点击「**加载已解压的扩展程序**」（英文是 `Load unpacked`）。
5. 在弹出的文件选择窗口中，选择**第二步解压出来的文件夹**（注意要选中文件夹本身，不要进去选里面的文件）。
6. 点击「选择文件夹」。

如果一切顺利，扩展列表里会出现一个名为「**Ajax Interceptor Tools**」的插件卡片，说明安装成功！

---

## 第四步：如何使用

1. 打开你要调试的网页。
2. 找到浏览器右上角工具栏上的扩展图标（名为「**Ajax Interceptor Tools**」），点击它。
3. 工作台面板会从页面右侧滑入，你就可以开始配置拦截规则了。
4. 再次点击扩展图标（或点击面板左上角的关闭按钮）即可隐藏面板。

> 💡 如果找不到扩展图标：点击浏览器工具栏右上角的拼图图标（扩展程序），在列表中找到「Ajax Interceptor Tools」，点击它旁边的图钉图标将其固定到工具栏，方便以后使用。

---

## 常见问题

### Q1：加载时报错「找不到 manifest.json」？
说明你选错了文件夹。请确认选的是**解压后**的文件夹，并且这个文件夹里能直接看到一个叫 `manifest.json` 的文件。

### Q2：插件安装了，但点击图标后面板没出现？
1. 确认当前打开的是普通网页（`http://` 或 `https://` 开头），而不是 `chrome://` 之类的浏览器内置页面。
2. 刷新当前网页后再点击扩展图标。
3. 如果还是没有，回到 `chrome://extensions` 页面，点击插件卡片上的「刷新」按钮（圆形箭头图标），然后刷新网页重试。

### Q3：换了电脑怎么办？
重复上面三步即可。本插件的配置数据保存在 Chrome 本地，不会自动同步到新电脑。

### Q4：怎么更新到新版本？
1. 在 `chrome://extensions` 页面，先移除旧版插件。
2. 下载并解压新版本的 zip 文件。
3. 按第三步重新加载新文件夹。

### Q5：能不能不删旧文件夹直接覆盖更新？
可以，但建议先移除旧插件，再加载新文件夹，避免缓存导致的问题。

---

## 卸载方法

如果不再需要这个插件：
1. 打开 `chrome://extensions`。
2. 找到「Ajax Interceptor Tools」卡片。
3. 点击卡片右下角的「移除」按钮。
4. 确认移除即可。

---

## 小贴士

- 插件文件夹**不能删除或移动**，否则插件会失效。建议放到固定位置。
- 如果电脑重启后插件不见了，回到 `chrome://extensions` 重新启用即可。
- 使用过程中遇到问题，可以截图发给开发同事协助排查。

## 功能介绍与使用教程

装好插件后，它能帮你做什么？本节用一个完整的真实场景，带你从零配出第一条规则。

### 插件能做什么（功能总览）

简单说，这个插件能在网页发出请求时「半路拦截」，让你**不改代码、不依赖后端**就能：

- **Mock 接口**：把后端返回的内容替换成自定义数据（让列表永远显示 3 条、让某个接口永远报错等）
- **改写请求**：修改请求地址、请求方法、请求头，或改写请求参数
- **管理规则**：把规则按「分组」整理，一键开关，互不干扰
- **导入 / 导出**：把配好的规则存成文件，换电脑、传给同事都能用
- **页面请求头**：给某个网站统一加上调试用的请求头（如 `x-debug-mode: 1`）

> 一句话理解：它就像一个「请求翻译官」，站在网页和后端之间，按你的规则改写发出的请求或返回的响应。

---

### 第 1 步：创建分组（以及为什么要分组）

「分组」就像是装规则的文件夹。建议按**调试场景**或**业务模块**来建分组，以后找规则、开关规则都方便。

**怎么建分组：**
1. 点击插件图标打开工作台。
2. 点击顶部的 `Create Group`（或在左侧分组导航点 `Add`）。
3. 给分组起个一眼能看懂的名字，比如 `商品详情页 Mock`。

**常见分组场景：**

| 分组名 | 什么时候用 |
| --- | --- |
| `商品详情页 Mock` | 页面还没开发完，先用假数据联调 UI |
| `空状态测试` | 测试列表为空时页面展示是否正常 |
| `错误分支验证` | 验证接口报错时的兜底提示和重试 |
| `TEMP-XXX调试` | 临时改某个接口数据，用完就删 |

> 💡 小技巧：临时分组用 `TEMP-`、`DEBUG-` 开头，方便日后识别和清理。

---

### 第 2 步：在分组里创建规则

规则是真正干活的那一条配置，一个分组里可以放多条。

**怎么创建规则：**
1. 选中刚才建好的分组。
2. 点击 `Add Rule`（新增规则）。
3. 填写关键字段：
   - **匹配类型（Match Type）**：地址固定选 `normal`；地址带可变部分（如订单号）选 `regex`。
   - **方法（Method）**：选 `GET` / `POST` 等；不确定就留空（匹配任意方法）。
   - **请求匹配条件（Request Matcher）**：填要拦截的接口地址，例如 `/api/order/list`。
   - **备注（Rule Notes）**：写明规则目的，比如「强制购物车为空」。
4. 填好后点击 `Enable`（启用）这条规则。

---

### 第 3 步：如何 Mock 一个现有接口（完整示例）

这是最常用的场景。假设后端有个「用户信息」接口，你想把它替换成自定义数据来测前端展示。

**完整步骤：**
1. **新建分组**：起名 `用户中心 Mock`。
2. **新建规则**：匹配条件填接口地址，例如 `https://api.example.com/user/profile`，方法选 `GET`。
3. **配置响应**：在规则卡片上点 `Response`，弹窗里：
   - 语言模式选 `json`
   - 状态码填 `200`
   - 响应体填你想要的假数据：

   ```json
   {
     "status": 200,
     "response": {
       "name": "测试用户",
       "role": "admin",
       "level": "VIP"
     }
   }
   ```

4. **启用规则**：点 `Enable`。
5. **验证效果**：刷新网页，接口返回的已经是你的假数据了（可在浏览器 `Network` 面板查看）。

**其他常用 Mock 套路：**

- **模拟空列表**：响应体写成 `{ "status": 200, "response": [] }`，用来测空状态页。
- **模拟接口报错**：状态码改成 `500`，响应体写成错误结构，用来测错误兜底和重试。
- **动态生成数据**：语言模式选 `javascript`，用循环造一批假数据（如 100 条列表项），适合测长列表和分页。

---

### 第 4 步：导入与导出配置

配好的规则可以保存成 JSON 文件，方便备份、换电脑或分享给同事。

入口在左侧操作栏顶部的 **`Import / Export`**，点开后是一个弹窗，分上下两块。

**导出（备份 / 分享规则）：**
- `Export all rules` —— 导出全部规则。
- `Export current group` —— 只导出当前分组。
- `Download a JSON template` —— 下载一个空白模板，了解文件格式。

> 建议：大改动、删分组、切环境前，先 `Export all rules` 留底。

**导入（恢复 / 共享规则）：**
1. 在弹窗下半部分，把 JSON 文件拖进去（或点击选择），支持多选。
2. 文件会先解析校验，列表里显示绿勾（成功）或红叉（格式有误）。
3. 点击 `Import`（或 `Import N group(s)`）完成导入。
4. 导入的分组会**追加**在现有规则后面，不会清空你已有的配置。

> ⚠️ 注意：导入前最好先导出一份当前规则做备份，避免合并后混乱。

---

### 第 5 步：其他实用功能

- **页面请求头（Headers）**：给某个网站统一加请求头。点左侧 `Headers`，开启后填键值对（如 `x-debug-mode` = `1`）保存即可。
- **域名白名单（Domain Whitelist）**：控制插件只在指定网站生效，避免误拦其他站点。默认 `*` 表示所有网站都生效。
- **悬浮规则面板（Floating Rules）**：不用展开整个工作台，在页面右下角的小浮窗里就能快速开关规则，还能拖动位置。
- **CSR 模式**：通过 `__csr=1` 参数切换当前页面的客户端渲染模式，方便对比 SSR / CSR 表现。

---

> 如需了解更深入的技术细节（请求改写、请求体脚本、响应改写机制等），请参考 [README-ZH.md](./README-ZH.md)。
